-- Migration Script

BEGIN TRANSACTION

CREATE TABLE [Demo].[WorkHistory]
(
	[WorkHistoryId] UNIQUEIDENTIFIER NOT NULL DEFAULT (NEWSEQUENTIALID()) PRIMARY KEY,
    [PersonId] UNIQUEIDENTIFIER NOT NULL, 
    [Name] NVARCHAR(100) NOT NULL, 
    [StartDate] DATE NOT NULL, 
    [EndDate] DATE NULL
)
	
COMMIT TRANSACTION